<h1>客戶 案例 管理</h1>

<p id="notice"><%= notice %></p>

<table class="table">
  <thead>
    <tr>
      <th>經銷商</th>
      <th>負責業務</th>
      <th>區域</th>
      <th>案例</th>
      <th>成交</th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= @lead.customer %></td>
        <td><%= @lead.sales %></td>
        <td><%= @lead.area %></td>
        <td><%= @lead.opp %></td>
        <td><%= @lead.order %></td>
      </tr>
  </tbody>
</table>



<%= link_to 'Edit', edit_lead_path(@lead), class: 'btn btn-info' %>
<%= link_to 'Back', leads_path, class: 'btn btn-success' %>


